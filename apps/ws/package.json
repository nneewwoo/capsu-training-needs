{
  "name": "@seminar-assess/api-ws",
  "version": "0.1.0",
  "scripts": {
    "dev": "NODE_ENV=development bun --watch src/index.ts",
    "build": "bun build src/index.ts --target bun --minify --outdir ./dist",
    "start": "NODE_ENV=production bun dist/index.js"
  },
  "dependencies": {
    "hono": "^4.6.16"
  },
  "devDependencies": {
    "bun-types": "^1.1.43"
  }
}
